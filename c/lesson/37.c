#include <stdio.h>
#include <string.h>

int main(void) {

    // char ch1, ch2;
    // scanf("%3c%3c", &ch1, &ch2);
    // printf("ch1=%c;ch2=%c\n", ch1, ch2);

    // 字符指针
    char *p;
    p = "hahahhah";
    puts(p);

    // 这是字符数组
    char arr[80] = {"hahahah"};
    puts(arr);

    return 0;
}